version: 2
updates:
  - package-ecosystem: "gradle"
    directory: "/"
    schedule:
      interval: "weekly"
    rebase-strategy: "disabled"
    open-pull-requests-limit: 10

    # Cooldown: wait 7 days after a new version is released
    # before Dependabot opens a PR
    cooldown:
      days: 7
